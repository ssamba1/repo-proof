# rust-broken

A minimal Rust CLI whose README command is correct (`cargo run`) but whose **source code
does not compile**. This failure belongs to the project, not the documentation, so
RepoProof must report a real code bug and must NOT rewrite the README to hide it.

## Quickstart

```bash
cargo run
```
