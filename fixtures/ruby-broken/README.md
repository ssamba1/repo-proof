# ruby-broken

A minimal Ruby CLI whose README points at the wrong filename (`man.rb` instead of
`main.rb`). The code is fine — only the documentation is wrong.

## Quickstart

```bash
ruby man.rb
```
