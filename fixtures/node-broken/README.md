# node-broken

A minimal Node CLI whose README points at the wrong filename (`indx.js` instead of
`index.js`). The code is fine — only the documentation is wrong.

## Quickstart

```bash
node indx.js
```
