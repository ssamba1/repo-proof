# py-broken

A minimal Python CLI whose README points at the wrong filename (`man.py` instead of
`main.py`). The code is fine — only the documentation is wrong, so RepoProof should fix it.

## Quickstart

```bash
python man.py
```
